id: p1_18_dense_temporal_readout
priority: P1
claim: role_separated_routing
control: active_compute_control
formal_use: primary
base_config: ../../configs/seal_small_echoset.yaml
question: Is sparse expert specialization useful compared with a dense temporal readout at comparable active computation?
comparison: p0_00_full_m1_stepbound
overrides:
  model:
    name: "ABLATION P1-18 | Dense temporal readout (active-MAC control)"
  network_config:
    moe_enabled: false
    # Structural prerequisite: there is no router embedding to bound.
    step_embedding_max_norm: 0.0
  trainer:
    exp_path: exp/ablation_m1_stepbound/p1_18_dense_temporal_readout
